language: cpp

addons:
  apt:
    packages: 
    - gfortran
    - libblas-dev
    - liblapack-dev

compiler:
  - gcc

script: 
 - make -e realclean
 - bash make.sh -cp2k -j
 - make -e samples
 - make -e test
 - bash test.sh
